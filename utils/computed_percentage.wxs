// 这里写wxs代码  将消息格式化  给的数字 显示对应的文字

var c_percentage = function (num1,num2) {

  var result = 0
  if (num1 == 0 && num2 == 0) {
    return 0
  }
  result = Math.floor((num1/(num1+num2))*100)
  return result
}

module.exports = {
  c_percentage:c_percentage
};